本次更新优化单文件包的启动资源，并修复精简构建中背包识别提示 `No module named 'numpy.ma'` 的问题。

- 恢复背包网格定位和数量识别所必需的 `numpy.ma`，其他用户无需安装 Python 或额外依赖。
- 裁剪未使用的图片格式插件、NumPy 可选模块及 Tcl 时区数据，减少单文件体积和启动时解压的文件数量，仍保持单 EXE 使用方式。

遇到上述报错的用户，请退出旧程序，下载下方 `MeteorCounter.exe` 覆盖使用；历史统计、球图标和单价保留。
