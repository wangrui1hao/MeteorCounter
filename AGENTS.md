# 项目工作约定

先读 [README.md](README.md) 了解使用方式，再读 [开发指南](docs/DEVELOPMENT.md) 和 [架构说明](docs/ARCHITECTURE.md)，以实际工作目录为准。

## 用户约束

- 用户可能在玩游戏。不要控制鼠标/键盘/焦点、捕获实时桌面、启动可见UI、播放声音或结束无关进程。不自动运行 scripts/run.ps1。
- 获准启动工具或处理真实数据时，须使用无 Windows 包身份的进程（GetCurrentPackageFullName 返回 15700）；Codex 的 MSIX 上下文会重定向 AppData，可能产生第二套数据库并共用 SQLite 日志文件。不要直接从该上下文启动工具或打开真实 SQLite 数据；恢复前备份，并在正常用户上下文核对结果。
- 工具只读窗口位置和屏幕像素，不读游戏内存、不注入、不操作游戏。
- 诊断截图每次启动默认关闭，用户确认后仅本次运行开启；关闭须阻止后续补帧。统计、日志和学习球图标独立保存，不把关闭截图描述为停止识别或删除历史。参见 docs/PRIVACY.md。
- 唯一发布位置为 publish 下的单文件EXE，不额外生成压缩包、SHA、版本目录、桌面入口或 runtime 副本。更新前检查程序是否退出；未经当前使用者明确授权，不结束其运行进程。
- 不清空、改写或合并 AppData 真实统计；开发操作使用隔离临时数据。程序的自动清理规则：正常截图 3 天、失败截图 7 天、日志 7 天，截图合计 500 MiB，保护正在写入的文件；当前轮次的过旧截图也可清理。旧数据迁移需校验，不覆盖已有用户数据库。
- 只有用户手动“开始新一轮”清零。界面“清理缓存”经确认合并清理已结束轮次及完成截图、旧日志和闲置解压目录，保留当前轮次、当前截图、球图标和单价。
- 固定严格识别，保持单页双列、球图标＋初始＋消耗＋可编辑单价；单价为正整数、输入 0 清除为未设置、跨轮次保留，总成本按消耗乘单价求和，未设置单价按 0 计算，总成本始终显示数值。不要恢复名称、导出、剩余、补充、识别预览或灵敏度选项。

## 开发与交付

- 版本发布需用户明确授权。授权后同步版本号、验证并提交推送，按当前仓库的发布流程操作；普通本地调整不自动发布。
- 每次发版须编写 docs/releases/vX.Y.Z.md，仅用中文说明本次版本的调整和修复，不加入验证结果或通用更新说明。发布流程读取该文件；不得仅使用自动生成的比较链接作为说明。

- 新机器按开发指南的 setup → build 准备和验证；必须保持 clone 后无需额外收集运行素材即可构建可用 EXE。通过 common.ps1 的 Get-ProjectPython 选择 AppData 独立环境，复用系统 Python 3.12 x64；不写本机绝对路径、不复制跨机器环境、不恢复 .tools/.venv。
- 版本同步 src/diagnostics.py 与 src/windows_version.txt，依赖和流程变更同步清单与文档。
- README 只放面向使用者的说明；环境和发布流程写开发指南，实现契约写架构说明，Agent 约束写本文。artifacts 只存生成结果，不堆叠交接和逐次排查报告。
- 当前为公开仓库，仅提交工具源码、运行资源和必要文档，不提交本机数据或旧仓库历史；变更远端或仓库可见性须获得维护者明确授权。运行素材随仓库保留，版本标签触发 Release 构建与上传，普通 CI 只验证构建。复用本地脚本，遵守发布版本校验，不把个人凭据、本机历史或 publish 文件提交到 Git。
